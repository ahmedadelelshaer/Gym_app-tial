import 'package:flutter/material.dart';

class LegTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Leg Tab"),
      ),
    );
  }
}
